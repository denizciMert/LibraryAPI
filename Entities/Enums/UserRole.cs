namespace LibraryAPI.Entities.Enums
{
    // UserRole enum class model
    public enum UserRole
    {
        // Visitor role
        Ziyaretçi = 0,

        // User role
        Kullanıcı = 1,

        // Employee role
        Çalışan = 2,

        // Administrator role
        Yönetici = 3
    }
}